export const metadata = {
  title: "Authentication | RentBari",
  description:
    "Sign in or create an account to access your RentBari dashboard, manage properties, bookings, and rental activities.",
};

const AuthLayout = ({ children }) => {
  return <div>{children}</div>;
};

export default AuthLayout;
